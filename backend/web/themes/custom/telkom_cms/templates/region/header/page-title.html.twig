{{ title_prefix }}
{% if title['#markup']|length > 0 %}
    <a class="navbar-brand col-md-3 col-lg-2 me-0 fw-bold" href="{{ redirect }}">{{title}}</a>
{% else %}
    <a class="navbar-brand col-md-3 col-lg-2 me-0 fw-bold" href="{{ redirect }}">Telkom CMS</a>
{% endif %}
{{ title_suffix }}
